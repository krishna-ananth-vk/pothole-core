name: Deploy Go Backend

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: "1.22"

      - name: Decode and write Firebase key
        run: |
          echo "$FIREBASE_KEY_B64" | base64 --decode > serviceAccountKey.json

      - name: Build Go binary
        run: GOOS=linux GOARCH=amd64 go build -o backend .

      - name: Deploy via SCP
        uses: appleboy/scp-action@v0.1.3
        with:
          host: ${{ secrets.VM_IP }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          source: |
            backend
            serviceAccountKey.json
          target: "/opt/deployments/pothole/pothole-core"

      - name: Restart backend service
        uses: appleboy/ssh-action@v0.1.10
        with:
          host: ${{ secrets.VM_IP }}
          username: ${{ secrets.VM_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          script: |
            sudo systemctl restart pothole-core.service
